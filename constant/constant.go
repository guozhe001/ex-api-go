package constant

const (
  ConfigApiKey = "config/config_api_key.toml"
)

// 密钥配置文件的key
const (
  ApiKey = "api_key"
  Secret = "secret"
)

// 交易所
const (
  ExchangeBinance = "binance"
  ExchangeBithumb = "bithumb"
)
